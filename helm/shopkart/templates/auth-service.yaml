apiVersion: v1
kind: Service

metadata:
  name: auth-service

spec:
  selector:
    app: {{ .Release.Name }}-auth

  ports:
    - port: {{ .Values.auth.port }}
      targetPort: {{ .Values.auth.port }}

  type: ClusterIP