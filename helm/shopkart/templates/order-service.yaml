apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: {{ .Values.namespace }}
  labels:
    app: order-service

spec:
  type: ClusterIP

  selector:
    app: order-service

  ports:
    - port: {{ .Values.order.port }}
      targetPort: {{ .Values.order.port }}
      protocol: TCP